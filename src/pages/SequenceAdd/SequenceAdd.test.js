import React from 'react'
import { shallow } from 'enzyme'
import SequenceAdd from './SequenceAdd'

describe('<SequenceAdd>', () => {
    it('renders without errors', () => {
        shallow(<SequenceAdd />)
    })
})
