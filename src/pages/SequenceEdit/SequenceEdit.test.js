import React from 'react'
import { shallow } from 'enzyme'
import SequenceEdit from './SequenceEdit'

describe('<SequenceEdit>', () => {
    it('renders without errors', () => {
        shallow(<SequenceEdit />)
    })
})
